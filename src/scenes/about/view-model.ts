import {
  AboutViewModelInitProps as ViewModelInitProps,
  AboutViewModelProps as ViewModelProps,
} from "./props";

const useAboutViewModel: (props: ViewModelInitProps) => ViewModelProps = () => {
  return {};
};

export default useAboutViewModel;
