import {
  SkillsViewModelInitProps as ViewModelInitProps,
  SkillsViewModelProps as ViewModelProps,
} from "./props";

const useSkillsViewModel: (
  props: ViewModelInitProps,
) => ViewModelProps = (props) => {
  return {};
};

export default useSkillsViewModel;
