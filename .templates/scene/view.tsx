import React from "react";
import {
  __templateNameToPascalCase__ViewModelProps as ViewModelProps,
  __templateNameToPascalCase__ViewProps as ViewProps,
} from "./props";

const __templateNameToPascalCase__View = ({ viewModel }: ViewProps) => {
  const {} = viewModel;

  return "__templateNameToPascalCase__";
};

export default __templateNameToPascalCase__View;
